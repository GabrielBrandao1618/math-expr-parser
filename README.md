# Math expr parser

Para utilizar, basta executar o programa passando uma string com alguma expressão matemática:

```bash
math-expr-parser "1 + 5"
```

As operações aceitas são adição, subtração, multiplicação e divisão, e parênteses são respeitados no cálculo
